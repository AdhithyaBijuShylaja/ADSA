RETURN COMMENTS
1. Create a query that returns a list of all orders along with the name of the employee who processed each order

from Orders as o
load o.Employee as e
select {
    OrderId: id(o),
    EmployeeName: e.LastName
}

2.Create a query that returns a list of all employees and their superiors

from Employees as e
load e.ReportsTo as s
select{
    EmployeeLastName: e.LastName,
    SupervisorLastName: s.LastName
}

3.Create a query that returns a list of all products and their suppliers

from Products as p
load p.Supplier as s
select {
    Products: p.Name,
    Suppliers: s.Name
}


4.Create a query that returns a list of all products and their Category

from Products as p
load p.Category as s
select {
    Products: p.Name,
    Category: s.Name
}

5.//list of all discontinued products ordered by how many units remains in stock from most to least
//including products out of stock
from Products as p
where p.Discontinued = true
order by p.UnitsInStock desc
select {
    ProductName: p.ProductName,
    UnitsInStock: p.UnitsInStock,
    Discontinued: p.Discontinued
}

6.Create a query that returns a list of all orders with the name of the company who made the order.

from Orders as o
load o.Company as c
select {
    Order: id(o),
    CompanyName: c.Name
}



